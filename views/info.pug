extends layout

block content
  script(type='text/javascript', src='public/javascripts/localstorage.js')
  h1= title
  p Search vendor in ANAF database:
  form(action="/", method="post")
    label(for="input") Enter tax code: 
    input(type="text", id="input", name="codfiscal", required)
    input(type="submit", value="Submit")

  p Codul fiscal căutat: 
    i #{datefirma}