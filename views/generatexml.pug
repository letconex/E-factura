extends layout

block content
    //- script(type='text/javascript', src='/public/javascripts/gettva.js')
    p.message #{message}
        i #{taxdata}
    select
        each variable in [taxdata]
            option(value=variable) #{variable}
        select
        each variable in [taxdata]
            option(value=variable) #{variable}
    p Generați factura în format XML:
    form(action="/generatexml", method="post")
        label(for="input") Enter invoice data: 
        input(type="text", id="input", name="taxdata", required)
        input(type="submit", value="Submit")

    a(href="/images/logo.jpg" download="logo")
        img(src="/images/myw3schoolsimage.jpg" alt="W3Schools" width="104" height="142")